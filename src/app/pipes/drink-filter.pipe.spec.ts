import { DrinkFilterPipe } from './drink-filter.pipe';

describe('DrinkFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DrinkFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
